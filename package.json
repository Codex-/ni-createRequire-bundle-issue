{
  "name": "ni-createRequire-bundle-issue",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "scripts": {
    "build": "pnpm run build:types && pnpm run build:bundle",
    "build:bundle": "node ./esbuild.config.mjs",
    "build:types": "tsc"
  },
  "devDependencies": {
    "@antfu/ni": "^0.23.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.2"
  }
}
